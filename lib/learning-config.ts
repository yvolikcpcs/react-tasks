export type LearningConfig = {
  aiMentorRole: string;
  aiContentLanguage: string;
};

export const learningConfig: LearningConfig = {
  aiMentorRole: 'Senior Programming Mentor',
  aiContentLanguage: 'English',
};
